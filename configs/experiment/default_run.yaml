# @package _global_
defaults:
  - override /submodule: [dataset, model, train, predict, analysis, visualize]
  - _self_

hydra:
  mode: RUN

experiment:
  key: default_run # what parameter are you experimenting with
  mode: RUN
  seed: 42