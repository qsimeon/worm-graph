preprocess:
  url: https://www.dropbox.com/s/p7y4uvr3gt79qqs/opensource_data.zip?dl=1
  zipfile: opensource_data.zip
  dataset: all # any dataset in utils.VALID_DATASETS
  raw_dir: /home/lrvnc/projects/worm-graph/data/raw # Not working with relative path (why?)
  resample_dt: 0.1 # The time interval (in seconds) to use for resampling; if None, no resampling is performed
  smooth: FFT 
  # None: No smoothing # FFT: Fast Fourier Transform, # TVR: Total Variation Regularization, # KA: Kalman, # KE: Kernel