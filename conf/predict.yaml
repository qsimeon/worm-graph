predict:
  tau_out: 2000 # number of future time steps to predict
  # constraint: tau_out: int > 0
  context_len: 1000 # length of the context window to use for prediction
  model: # a pretrained model to make predictions with
    checkpoint_path: models/example_saved_model.pt # path to a trained model checkpoint to load
  dataset:
    name: Kaplan2020 # dataset to make predictions on