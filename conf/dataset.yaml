dataset:
  name: Kato2015 #[Kato2015, Nichols2017, Skora2018, Uzel2022, Kaplan2020, Flavell2023, Leifer2023], or Custom
  num_named_neurons: all # Number of neurons to train the model with (positive integer or 'all')
  num_worms: all # Number of worms to train the model with (positive integer or 'all')
  seed: 42 # Seed for reproducibility (positive integer)
  save: False # Save the used dataset (True or False) => creates custom.pickle dataset inside data/processed/neural
