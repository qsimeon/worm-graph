model:
  type: NetworkLSTM # options: LinearNN, NetworkLSTM, NeuralCFC
  input_size: 302 # keep this fixed
  hidden_size: 128 # options: int > 0
  num_layers: 1 # keep this fixed
  loss: l1 # options: MSE, Huber, L1
