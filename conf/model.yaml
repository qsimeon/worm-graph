model:
  type: neural_cfc # options: linear, dense_cfc, neural_cfc, lstm, vae_lstm
  input_size: 302 # fixed
  hidden_size: 64 # options: int > 0
  num_layers: 2 # fixed
  loss: mse # options: MSE, Huber, L1
