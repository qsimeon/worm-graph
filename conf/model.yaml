model:
  type: lstm # options: linear, dense_cfc, neural_cfc, lstm, vae_lstm
  # linearNN isn't work well now -03/10/2023
  input_size: 302 # fixed
  hidden_size: 302 # options: int > 0
  num_layers: 3 # fixed
  loss: mse # options: MSE, Huber, L1
