model:
  type: NetworkLSTM # options: LinearNN, NetworkLSTM, DenseCFC, NeuralCFC, VariationalLSTM
  input_size: 302 # fixed
  hidden_size: 64 # options: int > 0
  num_layers: 1 # fixed
  loss: l1 # options: MSE, Huber, L1
