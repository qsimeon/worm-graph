analysis:
  dir: logs/hydra/example
  param_names: ["train.train_size"]
  param: null

hierarchical_clustering:
  group_by: four # None, three, four
  method: 'ward' # ward, average, complete, single
  metric: None # None, euclidean, cityblock, cosine, correlation...