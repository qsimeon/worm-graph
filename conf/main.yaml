defaults: 
  - preprocess
  - dataset
  - model
  - visualize
  - train
  # NOTE: WHEN ON OPENMIND, UNCOMMENT LINE BELOW TO RUN MULTIPLE SLURM JOBS
  - slurm_default
  - _self_
globals:
  use_residual: False
  shuffle: False # whether to shuffle worms
# If you want your primary config to override the values of configs
# from the Defaults List, append _self_ to the end of the Defaults List
hydra:
  mode: MULTIRUN # options: RUN, MULTIRUN
  job:
    chdir: false
  run:
    dir: logs/hydra/${now:%Y_%m_%d_%H_%M_%S}-${dataset.name}-${model.type}
  sweep:
    dir: logs/hydra/${now:%Y_%m_%d_%H_%M_%S}-${dataset.name}-${model.type}
    subdir: ${hydra.job.num}
  sweeper:
    params:
      # train.epochs: 100, 300, 500, 600
      # train.seq_len: 9, 99, 299, 499
      # train.k_splits: 2, 5, 9
      # train.tau_in: 1, 10, 100
      # train.tau_out: 1, 10, 100
      # model.type: neuralcfc, networklstm
      model.hidden_size: 128, 512
      # globals.shuffle: True, False
      # globals.use_residual: True, False
      # dataset.name: Nichols2017
      
